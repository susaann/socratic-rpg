# Chapter 5: Backpropagation: The Engine of Deep Learning

> **Recommended Resources**
> 
>   * 3Blue1Brown, _Neural Networks_ series, Episodes 3–4 — "What is backpropagation really doing?" and "Backpropagation calculus" (geometric and algebraic walkthrough)
>   * Andrej Karpathy, _"micrograd"_ — 25-line implementation of backpropagation in Python; watching him build it reveals the computational graph mechanism
>   * Andrej Karpathy, CS231n Lecture 4 — "Backpropagation and Neural Networks" (Stanford open courseware, covers the material in this chapter in full detail)
> 


* * *

> **Chapter Learning Objectives**
> 
> By the end of this chapter you should be able to:
> 
>   1. Apply the univariate and multivariate chain rules to composite functions
>   2. Construct a computational graph for a given expression and identify forward and backward operations at each node
>   3. Perform a complete backpropagation calculation on the 2→3→1 network introduced in Chapter 4, deriving gradients for all 13 parameters
>   4. Explain the Dead ReLU problem in terms of gradient blocking at the activation node
>   5. Derive the general matrix-form backpropagation update rule for a weight matrix: ∂L/∂W(l)=δ(l)(h(l−1))⊤\partial L / \partial \mathbf{W}^{(l)} = \boldsymbol{\delta}^{(l)} (\mathbf{h}^{(l-1)})^\top∂L/∂W(l)=δ(l)(h(l−1))⊤
> 


* * *

