## 3.3 Learning Rate: The Most Consequential Hyperparameter

### Why Learning Rate Matters

Learning rate η\etaη has a larger impact on training outcome than almost any other design choice. Three regimes:

**Too large** (η≫1/L\eta \gg 1/Lη≫1/L): each step overshoots the minimum, causing oscillation or divergence. For an LLL-smooth function, gradient descent diverges when η>2/L\eta > 2/Lη>2/L.

**Too small** (η≪1/L\eta \ll 1/Lη≪1/L): convergence is painfully slow. With a fixed compute budget, the optimizer may not find a good solution before training ends. Very small learning rates also cause parameters to stay near the random initialization, often a poor starting point.

**Appropriate** : smooth, steady decrease in loss converging to a good solution within the training budget.

### Learning Rate Schedules

In practice, the learning rate is not fixed — it follows a **schedule** that reduces it over training. The intuition: large steps early in training to explore the loss surface broadly; small steps late in training to refine the solution.

**Step decay** :

ηt=η0⋅γ⌊t/s⌋\eta_t = \eta_0 \cdot \gamma^{\lfloor t/s \rfloor}ηt​=η0​⋅γ⌊t/s⌋

where γ∈(0,1)\gamma \in (0,1)γ∈(0,1) is the decay factor and sss is the decay interval in steps. Example: η0=0.1\eta_0 = 0.1η0​=0.1, γ=0.1\gamma = 0.1γ=0.1, s=30s = 30s=30 epochs drops the learning rate from 0.10.10.1 to 0.010.010.01 at epoch 30 and to 0.0010.0010.001 at epoch 60. This schedule was used in the original ResNet training.

**Cosine annealing** (Loshchilov & Hutter, 2017):

ηt=ηmin⁡+12(ηmax⁡−ηmin⁡)(1+cos⁡ ⁣(πtT))\eta_t = \eta_{\min} + \frac{1}{2}(\eta_{\max} - \eta_{\min})\left(1 + \cos\\!\left(\frac{\pi t}{T}\right)\right)ηt​=ηmin​+21​(ηmax​−ηmin​)(1+cos(Tπt​))

The cosine profile decays slowly early and late, and rapidly in the middle. Setting ηmin⁡=0\eta_{\min} = 0ηmin​=0 gives a smooth decay from ηmax⁡\eta_{\max}ηmax​ to zero. SGDR extends this with periodic restarts: after reaching ηmin⁡\eta_{\min}ηmin​, reset to ηmax⁡\eta_{\max}ηmax​ and begin again. Restarts can help escape sharp local minima.

**Linear warmup** :

ηt={ηmax⁡⋅tTwarmupt<Twarmup(decay schedule)t≥Twarmup\eta_t = \begin{cases} \eta_{\max} \cdot \dfrac{t}{T_{\text{warmup}}} & t < T_{\text{warmup}} \\\ \text{(decay schedule)} & t \geq T_{\text{warmup}} \end{cases}ηt​=⎩⎨⎧​ηmax​⋅Twarmup​t​(decay schedule)​t<Twarmup​t≥Twarmup​​

At initialization, parameters are random and gradient directions are unreliable. Starting with a large learning rate can push parameters far from initialization and destabilize training. Warmup gradually increases η\etaη from near-zero to the target value over TwarmupT_{\text{warmup}}Twarmup​ steps, allowing the model to establish stable gradient directions before accelerating. The original Transformer paper (Vaswani et al., 2017) used warmup followed by inverse square-root decay:

ηt=dmodel−0.5⋅min⁡(t−0.5, t⋅Twarmup−1.5)\eta_t = d_\text{model}^{-0.5} \cdot \min(t^{-0.5},\; t \cdot T_\text{warmup}^{-1.5})ηt​=dmodel−0.5​⋅min(t−0.5,t⋅Twarmup−1.5​)

This schedule has been adopted in nearly every subsequent large language model.

* * *

