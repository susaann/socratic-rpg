## 2.5 Regularization as a Bayesian Prior

We noted that high-capacity models overfit. A classical remedy is to add a **regularization term** to the loss:

Lreg(θ)=R^(θ)+λΩ(θ)\mathcal{L}_{\text{reg}}(\theta) = \hat{R}(\theta) + \lambda \Omega(\theta)Lreg​(θ)=R^(θ)+λΩ(θ)

where Ω(θ)\Omega(\theta)Ω(θ) is a complexity penalty and λ>0\lambda > 0λ>0 controls the strength of regularization. Two common penalties:

  * **L2 (Ridge / weight decay)** : Ω(θ)=∥θ∥22=∑jθj2\Omega(\theta) = \|\theta\|_2^2 = \sum_j \theta_j^2Ω(θ)=∥θ∥22​=∑j​θj2​
  * **L1 (Lasso)** : Ω(θ)=∥θ∥1=∑j∣θj∣\Omega(\theta) = \|\theta\|_1 = \sum_j |\theta_j|Ω(θ)=∥θ∥1​=∑j​∣θj​∣



The Bayesian perspective makes the interpretation precise. In Bayesian inference, we maximize the **posterior** :

p(θ∣D)∝p(D∣θ)⋅p(θ)p(\theta \mid \mathcal{D}) \propto p(\mathcal{D} \mid \theta) \cdot p(\theta)p(θ∣D)∝p(D∣θ)⋅p(θ)

Taking the negative log:

−log⁡p(θ∣D)=−log⁡p(D∣θ)−log⁡p(θ)+const-\log p(\theta \mid \mathcal{D}) = -\log p(\mathcal{D} \mid \theta) - \log p(\theta) + \text{const}−logp(θ∣D)=−logp(D∣θ)−logp(θ)+const

Minimizing this is **maximum a posteriori (MAP) estimation**. The two terms are:

  * −log⁡p(D∣θ)-\log p(\mathcal{D} \mid \theta)−logp(D∣θ): the negative log-likelihood, i.e., the empirical risk
  * −log⁡p(θ)-\log p(\theta)−logp(θ): the negative log-prior, i.e., the regularization term



If the prior is Gaussian θ∼N(0,τ2I)\theta \sim \mathcal{N}(0, \tau^2 I)θ∼N(0,τ2I):

−log⁡p(θ)=12τ2∥θ∥22+const-\log p(\theta) = \frac{1}{2\tau^2}\|\theta\|_2^2 + \text{const}−logp(θ)=2τ21​∥θ∥22​+const

This is L2 regularization with λ=1/(2τ2)\lambda = 1/(2\tau^2)λ=1/(2τ2).

If the prior is Laplace p(θj)∝exp⁡(−∣θj∣/b)p(\theta_j) \propto \exp(-|\theta_j|/b)p(θj​)∝exp(−∣θj​∣/b):

−log⁡p(θ)=1b∥θ∥1+const-\log p(\theta) = \frac{1}{b}\|\theta\|_1 + \text{const}−logp(θ)=b1​∥θ∥1​+const

This is L1 regularization. L1 regularization produces sparse solutions (many θj=0\theta_j = 0θj​=0) because the Laplace prior places probability mass near zero while maintaining heavy tails that allow some parameters to be large.

**Practical implication** : regularization is not an ad hoc trick. It encodes a prior belief about what models look like. L2 regularization says "parameters are probably small, with Gaussian uncertainty." L1 regularization says "most parameters are zero, but some may be large." Choosing the right prior is a modeling decision, not a hyperparameter search.

* * *

