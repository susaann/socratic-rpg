# 视界探索者教团 · 教学对话存档

- **日期**：2026-06-19 15:30
- **章节**：第1章 可移植性和标准
- **导师**：格里高尔（Gregor）
- **对话轮数**：62
- **模型**：deepseek-v4-flash
- **Token 用量**：总 350.2K

---

### 格里高尔（Gregor）：

*格里高尔把烟灰缸往桌边推了推，目光落在你身上，声音带着点沙哑的温和*  

教材说汇编语言是低级语言——我问你，这种语言直接操作的是什么东西？

### 旅者：

硬件系统？

### 格里高尔（Gregor）：

*他点点头，烟灰在桌角轻轻磕了一下*

方向对了。那咱们再说得具体一点——教材说它“直接操作硬件”，你想想看，在汇编语言里写一条 `MOV AX, BX` 这样的指令，它直接改变的是什么具体的东西？

### 旅者：

电荷的位置？

### 格里高尔（Gregor）：

*他愣了一下，烟在指间顿住，嘴角微微抽动了一下*

电荷的位置……嗯，有意思。我第一次被人这么回答过。

*他把烟重新叼在嘴角，慢慢说*

这样想也有道理，电子确实在动。不过教材写得更实在一些——你看一条 `MOV AX, BX` 指令，它把 BX 寄存器的值复制到 AX 寄存器里。那我问你，**寄存器**和**内存单元**，这两个东西在硬件里算是同一类东西吗？

### 旅者：

难道不是吗？

### 格里高尔（Gregor）：

*他眯起眼睛看了你一会儿，把烟灰弹进缸里，声音不紧不慢*

好，那我换个角度问你——你刚才说“难道不是吗”，意思是你觉得寄存器和内存单元是一样的东西。那如果我说 `MOV AX, [1234]` 这条指令，是把**内存地址1234里的值**读到 AX 寄存器里——那请你告诉我，**寄存器**和**内存**这两个东西，在物理上分别藏在计算机的哪里？

### 旅者：

机箱里面？

### 格里高尔（Gregor）：

*格里高尔把烟从嘴角拿下来，低头看了一眼，轻轻笑了一声——那笑声里没有嘲讽，倒像是被自己的问题蠢到了*

你说得对。它们确实都在机箱里面。

*他抬起头，眼神认真了些*

我的意思是——寄存器在 **CPU内部**，是芯片里的一小块高速存储单元。而内存是**独立的内存条**，插在主板上，离CPU隔着一段距离。程序计数器、累加器这些寄存器，访问速度大概是内存的**几十到上百倍**。

你在写 `MOV AX, BX` 的时候，操作的是CPU内部的东西。你在写 `MOV AX, [1234]` 的时候，得跑到主板上去取数。速度差距，就这么来的。

*他重新把烟叼上，随口问了一句*

好，那现在我问你第二个概念——教材说低级语言“移植困难”。刚才我们说的这些寄存器名字，比如 AX、BX，是英特尔 x86 架构的。那我问你，如果把这段汇编代码搬到一台 ARM 处理器的手机上，会发生什么事？

### 旅者：

那可能不能用了吧

### 格里高尔（Gregor）：

*格里高尔点了点头，把烟灰缸往桌上推了推*

“不能用了”——这四个字说得很准。汇编指令和具体的处理器指令集是**一一绑定**的，换一个架构，所有指令名、寄存器名、寻址方式全都得重写。

*他向后靠了靠椅背*

好，刚才我们走完了低级语言的定义：它直接操作硬件，效率高，但移植困难。现在你想想，教材紧接着就说了——为了应对这种困境，人们造出了**高级语言**。它面向的是“问题解决”，而不是“某一款CPU”。

我问你一个实际问题：如果我用 C 语言写了一个计算器程序，在 Windows 电脑上编译通过了。现在我把同一份**源代码**复制到一台 Mac 电脑上——按照教材的说法，我能不能直接在那上面运行它？

### 旅者：

能啊

### 格里高尔（Gregor）：

*他正要低头点烟，听到你的回答，手在半空中停住了。他放下打火机，看着你*

你说“能”——那我追问你：你是把 .exe 文件直接拷过去双击，还是带着源代码过去，在 Mac 上**再编译一次**？

这两个动作差别很大。你想想教材里那个关键角色——**编译器**——在这两种操作里分别扮演了什么角色？

### 旅者：

解包 和打包的区别？不太明白

### 格里高尔（Gregor）：

*格里高尔把烟搁在烟灰缸上，两手交握放在桌沿，耐心地说*

不急，咱们换个角度想。操作系统不会直接认人的——你说它处理逻辑、处理运算，那它对的还是你自己的思考过程。可咱们聊的是——**编译器在这个链条里到底做了什么？**

按教材的说法，编译器做的事叫**翻译**：它把你的高级语言源代码，变成当前计算机能直接执行的机器指令。

*他伸出一只手指点了点桌面*

那我问你一个很直白的问题——如果你有一本中文食谱，你想让一个只懂英语的厨师照着做，你能不能直接把中文食谱递给他？

### 旅者：

不行，得翻译成英文

### 格里高尔（Gregor）：

*格里高尔露出一点笑意，伸手在桌上比划了一下*

中文食谱 → 翻译 → 英文菜谱 → 厨师照着做。翻译就是一个中间人，把输入转成对方懂的东西。

同样：高级语言源代码 → **编译器** → 特定计算机的机器码 → CPU执行。

*他把打火机拿起来又放下*

好，同一个问题换个方向——教材说C语言很强调可移植性。刚才你说的那个例子，同一份C程序在Windows和Mac上都能跑——那它靠的是什么？是不是源代码本身就很神奇，能在两种CPU上直接跑？

### 旅者：

也就是两台电脑有根据自己的cpu编写的一套底层语言来解读正规c语言吗

### 格里高尔（Gregor）：

*他盯着你看了三秒，然后慢慢把烟叼回嘴角，吐出一口烟后才开口*

……你这思路有意思。你不是在背教材——你是在脑子里自己重构机制。可以。

不过你刚才说的“规范”这个词，可以再精确一点点。C语言的源代码确实是一套**统一的规范**——我写 `int a = 1 + 2;`，无论在什么平台上，这个语法都一样。那问题来了——

CPU不认识 `int a = 1 + 2;`，它只认识自己能执行的机器指令。所以编译器干的事就是：**把同一份规范，翻译成不同CPU能懂的方言**。在x86上生成一套机器码，在ARM上生成另一套机器码——源代码本身一个字都不用改。

*他屈起一根手指敲了敲桌面*

那我问你：既然C语言靠编译器来实现跨平台，那汇编语言为什么不能这么干——写一份统一的“伪汇编”源代码，然后为每个平台配一个编译器，把它翻译成不同CPU的指令？

### 旅者：

因为汇编语言是底层直接操控硬件的语言？每台机器要求的就不一样？

### 格里高尔（Gregor）：

*格里高尔把烟灰缸推到自己面前，听完你的话之后安静了一小段时间，然后点了点头*

“每台机器要求的不一样”——你这句话说到了根子上。汇编语言的操作对象是**具体的寄存器名字、具体的内存地址范围、具体的指令编码**。x86的 `MOV AX, BX` 和 ARM 的 `MOV R0, R1`，从功能上都叫“复制”，但指令格式、寄存器编号、长度都不一样。你没法写一份“通用汇编源代码”来抽象掉这些差异——因为汇编就是**不抽象**的语言，它的卖点就是直接。

所以C语言的编译器能跨平台，靠的是**在高级语言和底层机器之间架了一层统一规范**——规范是统一的，翻译是平台相关的。

*他把烟从嘴上拿下来，露出一个浅浅的、几乎看不出来的笑*

行，刚才咱们走通了三个东西：低级语言、高级语言、编译器。它们之间的关系你基本理清了。

*他重新把烟叼回去，侧了侧头，像是准备转向下一个话题*

教材接下来讲的是**过程性编程**——这个提法你以前听过吗？用教材的话说，它强调的不是数据结构，而是“算法”，也就是“做什么步骤”。我问你个直觉问题：你在写一段程序的时候，第一反应是想“数据长什么样”，还是“先做哪一步、再做哪一步”？

### 旅者：

先做一步再做一步吧

### 格里高尔（Gregor）：

*他点了点头，烟头的火星微微亮了一下*

“先做一步再做一步”——这恰恰就是过程性编程的核心直觉。教材把它比喻成菜谱：先放油，再放葱，再炒肉——关注的是**操作步骤的顺序**，而不是葱和肉本身长什么样。

*他把烟灰轻轻磕掉，话锋一转*

那接下来教材说——当程序越来越大的时候，光靠“先做一步再做一步”会出问题：到处都是跳转和分支，代码变得像一碗意大利面，谁改谁头疼。为了解决这个问题，人们提出了**结构化编程**。

*他抬起头来看向你，眼神认真的像在问你借什么东西*

我问你一个具体的：教材说结构化编程把“分支限制为一小组行为良好的结构”——那你能不能试着说出C语言里的**三种**这样的结构？比如用来做判断的、用来做重复的——你先想一个最常用的就行。

### 旅者：

(我卡住了，请给我一点提示或降低难度)

### 格里高尔（Gregor）：

*格里高尔注意到你停顿了一下，把烟从嘴边拿开，朝你摆了摆手*

没事，我不该一下子让你说三个的。那我把问题缩一缩——结构化编程的核心，是说程序里的**流程控制**只用少数几种规范的结构来表达，不要到处乱跳。

我就问你一个最简单的：在C语言里，如果我想写一个“如果今天下雨，我就带伞；否则我就戴帽子”——这个**条件判断**需要用C语言里的哪个关键字来开头？

### 旅者：

如果？

### 格里高尔（Gregor）：

*他把烟重新叼回嘴角，轻轻点了点头，眼角的纹路显得温和了一点*

“如果”——对了。就是 `if` 语句。教材里说的“结构良好的流程控制”，最常见的就是 `if-else` 来做条件判断，`for`、`while` 来做循环，再加上顺序执行。C语言里就靠这几样东西，把程序的流向管得服服帖帖，不再像老代码那样用 `goto` 满天飞。

*他吐出一小口烟*

好，那我现在换个角度问你——教材说结构化编程采用“自顶向下”的设计方法。你用“整理书房”这个例子来想一想：如果你要整理一个很乱的书房，按自顶向下的思路，你会先画一个什么样的框架？比如第一步做什么、第二步做什么？

### 旅者：

先把书籍分类，再归堆，然后每一堆再分成小类，然后编号，把它们按顺序方上书架

### 格里高尔（Gregor）：

*格里高尔听完你的描述，把烟灰往缸里磕了磕，眼里闪过一丝赞许的光*

可以啊你。你这个“分类→归堆→再细分→编号→上架”的过程，其实就是一套完整的**自顶向下分解**：你把“整理书房”这个大任务，拆成了五个子步骤，而且这些子步骤是按照逻辑顺序排列的，彼此独立。教材里说的“将大型程序分解成小型、便于管理的任务”，你做了一次很好的类比。

*他往后靠了靠，声音缓了下来*

现在我们要转一个弯了。教材说，随着程序越来越大——比如几十万行的银行系统——光靠过程性+结构化那套“自顶向下”的思路也开始吃力了：数据和操作是分开的，你想改一个数据的格式，所有相关的函数都得跟着改，牵一发动全身。

*他敲了敲桌面*

于是有了**面向对象编程**——教材说它“强调数据”，先把“数据长什么样、数据能干什么”想清楚，再搭程序。我换一种方式问你：

你再想想你刚才说的整理书房——现在假设每本书都是一个“对象”，它有书名、作者、分类、位置这些**属性**，还有被抽出来、放回去这些**操作**。如果你给每本书都配上一个这样的**蓝图**——也就是教材所说的**类**——那你再整理书房时，第一反应是先想这些书长什么样，还是先去想整理步骤？

### 旅者：

长什么样吧

### 格里高尔（Gregor）：

*格里高尔把烟在缸沿上磕了磕，露出一个几乎看不见的弧度——像是确认你的方向对了*

“长什么样”——抓到重点了。OOP的第一反应是构建数据模型：一本书有什么属性（书名字数、编号页数、状态是否在架），有什么行为（被借走、被归还、被移动）。

*他微微侧过头*

好，教材在这块核心强调了两个词给你——**类**和**对象**。类是什么？用你的书房例子，如果你给“书”画一个蓝图，叫做“书这个类别里所有成员都有什么特征和什么行为”——这就是是类。对象呢？

你别直接定义它，拿一句话告诉我：一本具体的《高等数学》放在书架上——它是类，还是对象？

### 旅者：

对象吧

### 格里高尔（Gregor）：

*他点了点头，烟在嘴边停留了一下*

《高等数学》是你手里的具体实例，书名叫高数、作者是谁、在哪个书架上——都是确定的值。那就是**对象**。而“书”这个蓝图——也就是定义了书名、作者、状态这些字段的规范——那是**类**。

*他把烟拿下来，指向你*

好，那我再问你一个逻辑问题，把这块彻底理清——如果我有一本《离散数学》和一本《C++ Primer Plus》，它们都是对象。那它们**共享同一个**“书”这个类的定义对吧？但是它们存的数据……一样吗？

### 旅者：

不一样

### 格里高尔（Gregor）：

*格里高尔把烟叼在嘴角，眯起眼，轻轻点了点头——那是一种很淡的认可*

对了。两个对象共享同一个类定义——就像是都用同一张模具造出来的积木——但每块积木自己的颜色、大小、具体数值是独立的。

*他把烟从嘴上拿下，用指节轻轻叩了两下桌面*

那我现在问你一个把刚才这两层连起来的问题。你刚才说“把《高等数学》放到对象位置上”——那如果是类，它能不能摆在书架上？换句话说，你是真的能在物理世界里拿到一个“书”这个抽象概念本身，还是你只能拿到这种蓝图的产物——具体的那本书？

### 旅者：

那本书？

### 格里高尔（Gregor）：

*格里高尔把烟夹在指间，没有急着点，静静看了你两秒。然后嘴角轻轻动了动*

你刚才说“那本书”——我猜你指的是手里那本《高等数学》，那个能翻开、有重量、能放在包里的东西。那它当然是**对象**——它是根据“书”这个蓝图造出来的一个具体存在。

*他把烟叼回嘴里，含糊地补了一句*

但“书”这个**类**本身——也就是你定义的“书名、作者、状态、能借出、能归还”这组规范和操作——你把它放在书架上吗？一本看不见的书摆在那，你能借走它吗？

### 旅者：

做不到吧

### 格里高尔（Gregor）：

*格里高尔把烟从嘴边拿开，捏在指间，认真看着你。他沉默了两三秒，才开口*

“……行。你把这个区别吃进去了。”

*他把烟搁在烟灰缸边沿上，身体微微往前倾了一点*

那我问你下一个问题，把刚才那一整段连起来——教材说OOP采用的是**自下向上**的方式：你先定义数据模型（类），再根据类造出对象，最后用对象搭出整个程序。而你刚才整理书房用的是**自顶向下**：先拆成大任务，再拆成小任务。

那你现在想一想——假如我真用“书”这个自下向上的模型来建设图书馆管理系统，和之前用“先拆步骤”的自顶向下的方法，最大的不同在哪？

不要翻定义——就按你的直觉说。

### 旅者：

一个强调数据，一个强调算法

### 格里高尔（Gregor）：

*他把烟叼在嘴角，眼睛微微眯了一下。他沉默了大约两次呼吸的时间，然后开口，声音很轻*

“……你一句话把教材那一整段的核心拎出来了。”

*他把烟灰弹进缸里，然后抬头看你，目光里带了一点认真*

教材的原话是：**过程性编程强调算法（步骤），OOP强调数据（结构和行为）**。你用“一个强调数据，一个强调算法”压住了那个骨架。

*他把烟叼稳了，稍稍侧头*

好，那我们顺着节拍往下走。教材在讲完OOP之后接着讲了**泛型编程**。它说泛型编程的目标和OOP一样是“代码复用”，但侧重点不同：OOP强调数据，泛型编程强调**独立于类型**。

我举个具体的——如果你要写一个排序函数，不用泛型的话，你是不是得给`int`写一个版本，给`double`写一个版本，给`string`写一个版本……那问题来了，这几个版本除了类型不一样，逻辑一模一样。你用你刚才“强调数据”和“强调算法”的区别，想一想——泛型编程更关注的是数据，还是算法？

### 旅者：

算法吧

### 格里高尔（Gregor）：

*格里高尔把烟从嘴边拿开，轻轻点了点头*

“算法”——对了。泛型编程关心的是**逻辑**本身，而不是这个逻辑处理的数据是整数、浮点数还是字符串。教材说它让你“只编写一个泛型函数，并将其用于各种实际类型”。

*他停顿了一下，把烟灰磕进缸里，声音沉了点*

好，那我现在问你一个很实际的判断——假如你写一个`swap`函数，用来交换两个变量的值。你不用泛型的话，得给`int`写一个版本，给`double`写一个版本，给`string`写一个版本……每个版本的逻辑都在说同一句话：“把a的值给temp，把b的值给a，把temp的值给b”。而使用泛型——也就是C++里的**模板**——你只需要写一次。

那我现在问你：如果泛型编程只写一次，那它在背后是怎么做到自动适配不同类型的？别背术语，按教材给的线索和你的直觉说就行。

### 旅者：

它创建独立于类型的代码，通过模板实现？

### 旅者：

⏱️ 系统提示：学生此次回复耗时 161 秒（超过120秒阈值），可能在此知识点上存在困惑，请注意检测是否需要标记卡住。

### 格里高尔（Gregor）：

*格里高尔把烟夹在指间，没有急着点，目光从烟端上抬起来看向你。他停了停，然后慢慢开口——语气里没有评判，只是平静确认你刚才的内容*

“独立于类型的代码”——这是教材上的原话。“通过模板实现”——你把它挂住了。归纳得对。

*他把烟叼回嘴角，做了个“顺着这个方向再说一步”的手势*

那我再追问一个很窄的问题——教材说的“模板”，它到底在编译的时候干了什么，才能让同一个`swap`逻辑既处理`int`又处理`double`？就是问这个“独立于类型”的效果，到底是程序跑起来之后做的，还是编译器在编译阶段就已经替你处理完了？

### 旅者：

是编译阶段就完成了吧？

### 格里高尔（Gregor）：

*格里高尔把一个烟灰缸往旁边挪了挪，露出桌面上一个空白区域。他点了点头，声音不紧不慢——

“对，编译阶段就完成了——不是运行时动态去判断是什么类型，而是编译器根据你的调用，自动生成一份专门针对这种类型的版本。所以你写一次模板逻辑，编译器替你生成多个具体函数。”

*他把烟叼在嘴角，含糊地接着往下说*

“好，那接下来是教材最后一环。它说C++真正特殊的地方在于：它融合了三种范式——过程性、面向对象、泛型。而且它是在**C语言**的基础上加进来的，所以它和C语言的关系，教材用了一个非常关键的说法——C++是C的**超集**。

“我问你一个实际的问题——你写一个完全不用类、不用模板的纯C程序，比如就写一个 `printf("Hello\n");` ——这份代码在C++编译器上能不能过？”

### 旅者：

可以吧？

### 格里高尔（Gregor）：

*格里高尔轻轻把烟在缸沿上敲了两下，点了点头*

“可以吧”这三个字带个问号，但方向是对的——教材的原话是“任何有效的C程序都是有效的C++程序”，它的确能过。

*他把烟叼回嘴角，顿了一下*

那我最后问一个直的问题，把这一整节兜住。既然C++是C的超集，也就是你完全可以用C++只写C风格的过程性代码，完全不用类和模板——那教材为什么还是说“如果忽略面向对象特性，你将错过很多有用的东西”？

换句话说，C++在C之外多出来OOP和泛型这两个东西，到底解决了什么C语言自己搞不定的问题？你想想刚才咱们聊过的“牵一发动全身”和“给int、double各写一个排序函数”那两段，串起来说。

### 旅者：

(我卡住了，请给我一点提示或降低难度)

### 格里高尔（Gregor）：

*格里高尔看到你停住了，把烟从嘴边拿开，在指间转了转。他没有急着催你，声音平静下来*

没事，我把高度往下放一放。不和“解决了什么问题”这么大的框子较劲。

我先问你一个很具体的——咱们刚才说泛型编程的时候，你讲到了给每种类型各自写排序函数的事。那你想想，如果一个程序里需要排int、double、string，按C语言那套方式，你要写几个排序函数？

这个问题不需要你动脑子看大问题——就看着刚才咱们已经走过的那段路，说一个数就行。

### 旅者：

三个

### 格里高尔（Gregor）：

*格里高尔点了点头，把烟在缸沿上磕了磕*

三个。对。那我现在换个说法问你——这三个函数逻辑一模一样，只是处理的数据类型不同。那如果后来排序算法里发现有一个细节写错了，你要改几处？

你想想刚才咱们聊过的“牵一发动全身”那种感觉，在这事上锁不锁得住。

### 旅者：

三处？所以很难修改？

### 格里高尔（Gregor）：

*格里高尔听完你的话，停顿了一下，然后身子微微前倾，把烟在缸沿上慢慢摁灭——那个动作里没有焦躁，反而像是有意放慢节奏，让你跟上他的步调*

你刚才那句话——“三处”——然后“很难修改”，自己接上了。你把那个链条的最后一段推出来了。不加类、不写模板，你用纯C做统计项目的时候，改排序逻辑就必须挨个修正每个版本的函数体，漏一个就出现不一致。

*他松开按灭的烟，直起身重新翻向另一根烟，还没叼住，就半侧过头来看着你*

那正好——回到之前我们说泛型编程的时候，你提到的“写一次，编译器自动生成”。你现在看这个问题，它能帮上什么忙？

### 旅者：

这样就可以让所有的函数自动修改了

### 格里高尔（Gregor）：

*格里高尔把烟叼在嘴角，打火机在指间转了一圈，没有立刻点上。他点了下头，声音比刚才松弛了一点*

“……你把我正要说的那句堵住了。对——模板让你只维护一份逻辑，编译器替你复制。这就是泛型编程的实用意义。”

*他把打火机放回桌上，把烟从嘴上拿下来，往椅背上靠了靠*

好，那我们稍微停一下，把这一节走过的路在脑子里过一遍。

*他伸出手，用烟比划了一下*

一开始我们站在**低级语言和高级语言**的分岔口——你理解了为什么汇编直接操作硬件却难移植，为什么C语言通过编译器把同一份源代码翻译成不同平台的机器码。然后我们顺着**过程性编程**走到了**结构化编程**——知道了自顶向下拆解任务的好处。再接上**OOP**——你亲手分清了类和对象的区别，也理解了自下向上的思路。最后是**泛型编程**——你用“不用给每种类型各写一遍”这句话，把模板的实际价值兜住了。

*他把烟重新叼回嘴边，像是确认一般地补充了最后一句*

这三样东西——C语言的系统编程能力、OOP的数据建模能力、泛型的类型独立能力——全装在一个语言里，就是C++。

*他把烟搁在缸沿上，声音恢复成平常的随和语气*

这一节的核心内容我们算是走完了。你有什么要问的，或者我直接给你走结语？

### 旅者：

没了

### 格里高尔（Gregor）：

*格里高尔把烟从嘴边拿开，轻轻在缸沿上磕了一磕。他安静了两秒，然后才开口，声音里带着一种翻过一页书页的松动感*

好。那我们就到这儿。

*他朝你微微点了点头，那一点烟灰落在桌角上，他没有立刻拂掉*

### 旅者：

【系统提示】上一节已学完。现在开始教学下一节：第1章 1.3 可移植性和标准。以下是本节教材内容，请用苏格拉底教学法继续教学。

=== 教材内容 ===
假设您为运行Windows 2000的老式奔腾PC编写了一个很好用的C++程序，而管理人员决定用使用不同操作系统（如Mac OS X或Linux）和处理器（如SPARC处理器）的计算机替换它。该程序是否可以在新平台上运行呢？当然，必须使用为新平台设计的C++编译器对程序重新编译。但是否需要修改编写好的代码呢？如果在不修改代码的情况下，重新编译程序后，程序将运行良好，则该程序是可移植的。
在可移植性方面存在两个障碍，其中的一个是硬件。硬件特定的程序是不可移植的。例如，直接控制IBM PC 视频卡的程序在涉及Sun时将“胡言乱语”（将依赖于硬件的部分放在函数模块中可以最大限度地降低可移植性问题；这样只需重新编写这些模块即可）。本书将避免这种编程。
可移植性的第二个障碍是语言上的差异。口语确实可能产生问题。约克郡的人对某个事件的描述，布鲁克林人可能就听不明白，虽然这两个地方的人都说英语。计算机语言也可能出现方言。Windows XP C++的实现与Red Hat Linux或Macintosh OS X实现相同吗？虽然多数实现都希望其C++版本与其他版本兼容，但如果没有准确描述语言工作方式的公开标准，这将很难做到。因此，美国国家标准局（American National Standards Institute，ANSI）在1990年设立了一个委员会（ANSI X3J16），专门负责制定C++标准（ANSI制定了C语言标准）。国际标准化组织（ISO）很快通过自己的委员会（ISO-WG-21）加入了这个行列，创建了联合组织ANSI/ISO，致力于制定C++标准。
经过多年的努力，制定出了一个国际标准ISO/IEC 14882:1998，并于1998年获得了ISO、IEC（International Electrotechnical Committee，国际电工技术委员会）和ANSI的批准。该标准常被称为C++98，它不仅描述了已有的C++特性，还对该语言进行了扩展，添加了异常、运行阶段类型识别（RTTI）、模板和标准模板库（STL）。2003年，发布了C++标准第二版（IOS/IEC 14882:2003）；这个新版本是一次技术性修订，这意味着它对第一版进行了整理——修订错误、减少多义性等，但没有改变语言特性。这个版本常被称为C++03。由于C++03没有改变语言特性，因此我们使用C++98表示C++98/C++2003。
C++在不断发展。ISO标准委员会于2001年8月批准了新标准ISO/IEC 14882:2011，该标准以前称为C++11。与C++98一样，C++11也新增了众多特性。另外，其目标是消除不一致性，让C++学习和使用起来更容易。该标准还曾被称为C++0x，最初预期x为7或8，但标准制定工作是一个令人疲惫的缓慢过程。所幸的是，可将0x视为十六进制数，这意味着委员会只需在2015年前完成这项任务即可。根据这个度量标准，委员会还是提前完成了任务。
ISO C++标准还吸收了ANSI C语言标准，因为C++应尽量是C语言的超集。这意味着在理想情况下，任何有效的C程序都应是有效的C++程序。ANSI C与对应的C++规则之间存在一些差别，但这种差别很小。实际上，ANSI C加入了C++首次引入的一些特性，如函数原型和类型限定符const。
在ANSI C出现之前，C语言社区遵循一种事实标准，该标准基于Kernighan和Ritchie编写的《The C Programming Language》一书，通常被称为K&R C；ANSI C出现后，更简单的K&R C有时被称为经典C（Classic C）。
ANSI C标准不仅定义了C语言，还定义了一个ANSI C实现必须支持的标准C库。C++也使用了这个库；本书将其称为标准C库或标准库。另外，ANSI/ISO C++标准还提供了一个C++标准类库。
最新的C标准为C99，ISO和ANSI分别于1999年和2000年批准了该标准。该标准在C语言中添加了一些C++编译器支持的特性，如新的整型。
1.3.1 C++的发展
Stroustrup编写的《The Programming Language》包含65页的参考手册，它成了最初的C++事实标准。
下一个事实标准是Ellis和Stroustrup编写的《The Annotated C++ Reference Manual》。
C++98标准新增了大量特性，其篇幅将近800页，且包含的说明很少。
C++11标准的篇幅长达1350页，对旧标准做了大量的补充。
1.3.2 本书遵循的C++标准
当代的编译器都对C++98提供了很好的支持。编写本书期间，有些编译器还支持一些C++特性；随着新标准获批，对这些特性的支持将很快得到提高。本书反映了当前的情形，详尽地介绍了C++98，并涵盖了C++11新增的一些特性。在探讨相关的C++98主题时顺便介绍了一些C++新特性，而第18章专门介绍新特性，它总结了本书前面提到的一些特性，并介绍了其他特性。
在编写本书期间，对C++11的支持还不全面，因此难以全面介绍C++11新增的所有特性。考虑到篇幅限制，即使这个新标准获得了全面支持，也无法在一本书中全面介绍它。本书重点介绍大多数编译器都支持的特性，并简要地总结其他特性。
详细介绍C++之前，先介绍一些有关程序创建的基本知识。


### 格里高尔（Gregor）：

*格里高尔把烟灰往缸里磕了磕，靠在椅背上慢慢开口，声音里带着一种“翻过一页”的松弛感*

好，我们进了新一节。

这一节教材一开始就问了你一个很实际的问题——你给一台老式奔腾PC写了一个好用的C++程序，换了新机器（不同操作系统、不同处理器），这个程序还能不能跑？

你说“不能直接跑，得重新编译”——那我现在问你更深一层：假设重新编译后，程序的行为全都变了——原来是正确输出结果，现在输出乱码了——那教材对这种情况怎么判定的？它给“可移植性”下了什么定义？

